trigger:
- master

variables:
  # Azure Resource Manager connection created during pipeline creation
  azureSubscription: 'e0f5751b-983a-4d0c-a766-3ecda44e6b08'

  # Function app name
  functionAppName: 'spawnertest'

  # Working Directory
  workingDirectory: '$(System.DefaultWorkingDirectory)/spawner'

  # Deploy environment
  DEPLOY_ENVIRONMENT: 'test'

pool:
  vmImage: ubuntu-latest

stages:
  - template: pipeline-templates/zip-and-deploy-function.yml